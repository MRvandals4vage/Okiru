import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hunter.leveling',
  appName: 'HunterLevelingSystem',
  webDir: 'dist'
};

export default config;
